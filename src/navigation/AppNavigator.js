// src/navigation/AppNavigator.js

// Import necessary navigation components from React Navigation

// Create a functional component named AppNavigator
// This component will be the main navigation wrapper for the app
// It will contain the main navigation structure (e.g., Tab Navigator, Stack Navigator)

// Define the navigation structure using the imported components

// Export the AppNavigator component for use in other parts of the app
export default AppNavigator;